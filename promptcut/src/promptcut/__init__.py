"""PromptCut — a prompt-driven video editor built for Claude Code.

The editor has no GUI. A project is a JSON timeline (project.json) that an
agent (or a human) edits directly; the engine compiles it to ffmpeg commands.
"""

__version__ = "0.1.0"
